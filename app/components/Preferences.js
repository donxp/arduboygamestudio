let COMHelper = require('../util/COMHelper')

VTTCue.component('preferences', {
    template: `
    <div>
        <select name="COMPorts">
        <option value="x">x</option>
        </select>
    </div>
    `,//makes the template for page
})